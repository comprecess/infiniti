# ⛏️ Mining Monitor Bot

**Telegram-бот для ежедневного мониторинга прибыльности майнинга на ViaBTC с AI-аналитикой.**

Бот автоматически отслеживает прибыльность вашего майнинга BTC и LTC/DOGE на пуле ViaBTC, рассчитывает чистую прибыль с учётом расходов на электричество и предоставляет AI-рекомендации по управлению добытой криптовалютой.

---

## 🚀 Деплой на Ubuntu VPS за 5 минут

Этот гайд поможет вам быстро развернуть бота на вашем сервере.

### Шаг 1: Подключитесь к вашему VPS

```bash
ssh user@your_server_ip
```

### Шаг 2: Клонируйте репозиторий

```bash
git clone https://github.com/comprecess/infiniti.git
cd infiniti
```

### Шаг 3: Запустите интерактивный установщик

Скрипт `setup.sh` сделает всё за вас:
- Установит Docker и Docker Compose, если их нет
- Запросит ваши API-ключи и создаст `.env` файл
- Запросит список майнеров и создаст `config.json`
- Соберёт Docker-образ и запустит бота

```bash
./setup.sh
```

> **Всё!** Бот запущен и будет автоматически перезапускаться после перезагрузки сервера.

### Полезные команды

| Команда | Описание |
|---|---|
| `docker compose logs -f` | Смотреть логи бота в реальном времени |
| `docker compose restart` | Перезапустить бота |
| `docker compose down` | Остановить бота |
| `docker compose up -d` | Запустить бота в фоновом режиме |
| `nano .env` | Редактировать секретные ключи |
| `nano config.json` | Редактировать список майнеров |

---

## 📊 Возможности

- **Ежедневные автоматические отчёты** — полный отчёт каждый день в указанное вами время (по МСК)
- **Расчёт прибыльности** — доход минус расходы на электричество в USDT
- **Текущие курсы** — BTC, LTC, DOGE в реальном времени
- **Мониторинг хешрейта** — статус всех майнеров через ViaBTC API
- **AI-аналитика** — анализ рынка и рекомендации на основе GPT
- **Fear & Greed Index** — индикатор настроения рынка
- **История прибыльности** — тренды за последние 7/30 дней
- **Уведомления** — оповещения о важных изменениях

## 📱 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Главное меню с кнопками |
| `/report` | Полный отчёт о прибыльности с AI-анализом |
| `/prices` | Текущие курсы BTC, LTC, DOGE |
| `/hashrate` | Статус хешрейта на ViaBTC |
| `/ai` | AI-анализ рынка и рекомендации |
| `/history` | История прибыльности за 7 дней |
| `/settings` | Текущие настройки |
| `/set_electricity <цена>` | Изменить цену электричества |
| `/help` | Справка |

## 🔒 Безопасность

- **Секреты в `.env`**: Все ваши API-ключи хранятся в файле `.env` **только на вашем сервере** и никогда не попадут в Git.
- **Ограничение доступа**: Бот будет отвечать только на команды от `TELEGRAM_USER_ID`, указанного в `.env`.
- **IP Whitelist**: Для максимальной безопасности добавьте IP-адрес вашего VPS в белый список в настройках ViaBTC API.

## 📁 Структура проекта

```
infiniti/
├── setup.sh            # 🚀 Интерактивный установщик
├── bot.py              # Основной файл бота (команды, планировщик)
├── viabtc_api.py       # Клиент ViaBTC Pool API
├── price_api.py        # Клиент CoinGecko + blockchain.info
├── profitability.py    # Калькулятор прибыльности
├── ai_analyst.py       # AI-аналитик (OpenAI GPT + новости)
├── database.py         # SQLite база данных
├── config.json.example # Пример конфига с майнерами
├── requirements.txt    # Python зависимости
├── Dockerfile          # Docker образ
├── docker-compose.yml  # Docker Compose конфигурация
├── .env.example        # Пример файла с переменными окружения
├── .gitignore          # Git ignore
└── README.md           # Документация
```

## 📄 Лицензия

MIT License

---

*Разработано с помощью Manus AI*
