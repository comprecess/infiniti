import { RecentCard } from '../../../widgets/RecentCard/RecentCard'
import styles from './ProfileSettings.module.scss'

export const ProfileSettings = () => {
  return (
    <RecentCard title='Profile Settings' style={styles.recentFullScreen}>
      ProfileSettings
    </RecentCard>
  )
}
